// ===========================================
// AI变现之路 - System-Config字段描述自动配置脚本
// 为system-config内容类型添加完整的中文字段描述
// ===========================================

const fs = require('fs');
const path = require('path');

// 字段描述配置
const systemConfigDescriptions = {
  // 邮件服务配置
  emailServiceEnabled: "邮件服务总开关：是否启用邮件发送功能（注册验证、密码重置等）。true=启用邮件服务，false=禁用邮件服务。默认值：true",
  emailServiceProvider: "邮件服务商：选择使用的邮件服务提供商。gmail=Gmail邮箱服务，sendgrid=SendGrid专业邮件服务，aliyun=阿里云邮件推送，tencent=腾讯企业邮箱，mailgun=Mailgun邮件服务，custom_smtp=自定义SMTP服务器。默认值：gmail",
  emailSmtpHost: "SMTP服务器地址：邮件服务器主机名。示例：smtp.gmail.com、smtp.sendgrid.net、smtpdm.aliyun.com。必须与选择的邮件服务商匹配。默认值：smtp.gmail.com",
  emailSmtpPort: "SMTP端口：邮件服务器端口号。587=TLS加密传输（推荐），465=SSL加密传输，25=明文传输（不推荐）。范围：1-65535。默认值：587",
  emailFromAddress: "发件人邮箱：邮件发送方显示地址，收件人看到的发件人。格式：有效的邮箱地址。示例：noreply@aibianx.com、support@yourdomain.com。建议使用no-reply类型的邮箱。默认值：noreply@aibianx.com",
  emailFromName: "发件人姓名：邮件发送方显示名称，收件人看到的发件人名字。建议使用品牌名称或网站名称。最大长度：100个字符。默认值：AI变现之路",
  emailUseTLS: "使用TLS加密：是否启用传输层安全协议。true=启用TLS加密（推荐），false=不使用TLS加密（安全性较低）。默认值：true",
  emailSubjectPrefix: "邮件主题前缀：统一的邮件主题前缀，用于品牌识别。格式：[网站名称] 或 【网站名称】。示例：[AI变现之路]、【系统通知】。最大长度：50个字符。默认值：[AI变现之路]",
  
  // 用户注册配置
  registrationEnabled: "注册功能开关：是否允许用户注册新账号。true=开放用户注册，false=关闭用户注册（仅限邀请）。默认值：true",
  emailVerificationEnabled: "邮箱验证开关：注册时是否需要邮箱验证。true=需要邮箱验证（推荐），false=不需要验证（安全性较低）。默认值：true",
  emailVerificationCodeLength: "验证码长度：邮箱验证码的数字位数。范围：4-8位。建议：6位（安全性和用户体验的平衡）。默认值：6",
  emailVerificationCodeExpiry: "验证码有效期：邮箱验证码的有效时间（秒）。范围：60-3600秒（1分钟-1小时）。建议：600秒（10分钟）。默认值：600",
  passwordResetEnabled: "密码重置开关：是否启用忘记密码功能。true=用户可以重置密码，false=禁用密码重置功能。默认值：true",
  passwordResetTokenExpiry: "重置令牌有效期：密码重置链接的有效时间（秒）。范围：300-86400秒（5分钟-24小时）。建议：3600秒（1小时）。默认值：3600",
  
  // 密码策略配置
  passwordMinLength: "密码最小长度：用户密码最少字符数要求。范围：6-20位。建议：8位（安全性基本要求）。默认值：8",
  passwordRequireSpecialChar: "密码特殊字符要求：是否要求密码包含特殊字符。true=必须包含特殊字符（!@#$%^&*(),.?\":{}|<>），false=不强制要求特殊字符。默认值：true",
  passwordRequireNumber: "密码数字要求：是否要求密码包含数字。true=必须包含数字（0-9），false=不强制要求数字。默认值：true",
  passwordRequireUppercase: "密码大写字母要求：是否要求密码包含大写字母。true=必须包含大写字母（A-Z），false=不强制要求大写字母。默认值：false",
  
  // OAuth登录配置
  oauthEnabled: "OAuth登录总开关：是否启用第三方登录功能。true=启用第三方登录，false=禁用所有第三方登录。默认值：true",
  githubOauthEnabled: "GitHub登录开关：是否启用GitHub OAuth登录。true=用户可以使用GitHub账号登录，false=禁用GitHub登录。需要配置GitHub OAuth应用。默认值：true",
  googleOauthEnabled: "Google登录开关：是否启用Google OAuth登录。true=用户可以使用Google账号登录，false=禁用Google登录。需要配置Google OAuth应用。默认值：false",
  wechatOauthEnabled: "微信登录开关：是否启用微信OAuth登录。true=用户可以使用微信账号登录，false=禁用微信登录。需要配置微信开放平台应用。默认值：false",
  qqOauthEnabled: "QQ登录开关：是否启用QQ OAuth登录。true=用户可以使用QQ账号登录，false=禁用QQ登录。需要配置QQ互联应用。默认值：false",
  oauthAutoRegister: "OAuth自动注册：第三方登录时是否自动创建用户账号。true=自动创建新用户（推荐），false=仅允许已注册用户使用第三方登录。默认值：true",
  
  // 安全和会话配置
  sessionTimeout: "会话超时时间：用户登录会话的有效期（秒）。范围：3600-31536000秒（1小时-1年）。建议：2592000秒（30天）。默认值：2592000",
  maxLoginAttempts: "最大登录尝试次数：连续登录失败后锁定账号的次数限制。范围：3-10次。建议：5次（安全性和用户体验的平衡）。默认值：5",
  accountLockoutDuration: "账号锁定时长：登录失败次数达到上限后的锁定时间（秒）。范围：300-86400秒（5分钟-24小时）。建议：900秒（15分钟）。默认值：900",
  allowedEmailDomains: "允许的邮箱域名：限制注册邮箱的域名白名单。格式：一行一个域名，如：gmail.com。留空表示不限制。示例：gmail.com、qq.com、163.com",
  blockedEmailDomains: "禁止的邮箱域名：不允许注册的邮箱域名黑名单。格式：一行一个域名，如：tempmail.com。用于阻止临时邮箱和垃圾邮箱。示例：tempmail.com、10minutemail.com",
  
  // 系统维护配置
  maintenanceMode: "维护模式：是否启用网站维护模式。true=开启维护模式（用户将看到维护页面），false=正常运行模式。紧急情况下可立即启用。默认值：false",
  maintenanceMessage: "维护提示信息：维护模式下向用户显示的提示信息。最大长度：500字符。建议包含维护原因和预计恢复时间。默认值：网站正在进行系统升级维护，预计30分钟后恢复正常访问。",
  systemNotificationEmail: "系统通知邮箱：接收系统重要通知的管理员邮箱地址。格式：有效的邮箱地址。用于接收系统错误、安全警告等通知。默认值：admin@aibianx.com",
  
  // 用户功能配置
  enableUserProfileEdit: "用户资料编辑：是否允许用户修改个人资料信息。true=用户可以编辑个人资料，false=禁止用户修改资料。默认值：true",
  enableAccountDeletion: "账号删除功能：是否允许用户删除自己的账号。true=用户可以删除账号，false=禁止账号删除。涉及数据保护法规，请谨慎设置。默认值：true",
  defaultUserRole: "默认用户角色：新注册用户的默认权限角色。常用值：authenticated、public、user。建议：authenticated（已认证用户）。最大长度：50字符。默认值：authenticated",
  
  // 系统管理
  configurationNotes: "配置说明：管理员备注信息。用于记录重要的配置变更、注意事项等。最大长度：1000字符。支持多行文本。",
  lastModifiedBy: "最后修改人：记录最后一次修改此配置的管理员信息。系统自动记录。最大长度：100字符。用于审计和追踪。"
};

// 主函数
function updateSystemConfigDescriptions() {
  console.log('🚀 开始更新system-config字段描述...');
  
  try {
    // 读取配置文件
    const configFile = path.join(__dirname, 'strapi-config-dump.json');
    if (!fs.existsSync(configFile)) {
      throw new Error('配置文件不存在: strapi-config-dump.json');
    }
    
    const configData = JSON.parse(fs.readFileSync(configFile, 'utf8'));
    console.log(`✅ 成功读取配置文件，共 ${configData.length} 个配置项`);
    
    // 查找并更新system-config配置
    let updated = false;
    
    configData.forEach((item, index) => {
      if (item.key === 'plugin_content_manager_configuration_content_types::api::system-config.system-config') {
        console.log('🎯 找到system-config配置项，开始更新字段描述...');
        
        try {
          // 解析JSON值
          const value = JSON.parse(item.value);
          
          // 更新字段描述
          let fieldCount = 0;
          Object.keys(systemConfigDescriptions).forEach(fieldName => {
            if (value.metadatas && value.metadatas[fieldName] && value.metadatas[fieldName].edit) {
              value.metadatas[fieldName].edit.description = systemConfigDescriptions[fieldName];
              fieldCount++;
            } else {
              console.log(`⚠️  字段不存在或格式不正确: ${fieldName}`);
            }
          });
          
          // 保存更新后的配置
          item.value = JSON.stringify(value);
          
          console.log(`✅ 成功更新 ${fieldCount} 个字段描述`);
          updated = true;
          
        } catch (parseError) {
          console.error('❌ 解析system-config配置失败:', parseError.message);
          throw parseError;
        }
      }
    });
    
    if (!updated) {
      throw new Error('未找到system-config配置项');
    }
    
    // 保存更新后的配置文件
    const outputFile = path.join(__dirname, 'strapi-config-updated.json');
    fs.writeFileSync(outputFile, JSON.stringify(configData, null, 2));
    
    console.log('🎉 配置更新完成！');
    console.log(`📁 更新后的配置文件: ${outputFile}`);
    console.log('');
    console.log('📋 下一步操作：');
    console.log('1. 导入更新后的配置: npx strapi configuration:restore -f strapi-config-updated.json');
    console.log('2. 重启Strapi服务');
    console.log('3. 访问Admin界面验证字段描述');
    
  } catch (error) {
    console.error('❌ 配置更新失败:', error.message);
    process.exit(1);
  }
}

// 执行脚本
if (require.main === module) {
  updateSystemConfigDescriptions();
}

module.exports = { updateSystemConfigDescriptions, systemConfigDescriptions };