# AI变现之路项目 - NPM配置文件
# 防止在根目录意外安装依赖包

# 禁止在根目录安装包（防护措施）
# 项目依赖应该安装在frontend/、backend/或scripts/billionmail/mock-api/目录中
package-lock=false
save=false

# 如果需要安装依赖，请到对应目录：
# - 前端依赖: cd frontend && npm install
# - 后端依赖: cd backend && npm install  
# - BillionMail模拟API: cd scripts/billionmail/mock-api && npm install